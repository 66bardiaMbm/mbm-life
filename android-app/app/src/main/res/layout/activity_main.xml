<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Diagnostic-build-only control. It does not start/stop tracking or
         mutate sync state; it only exports the existing Room/WorkManager
         evidence needed to identify the cloud stall. -->
    <Button
        android:id="@+id/syncDiagnosticsButton"
        android:layout_width="wrap_content"
        android:layout_height="44dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="52dp"
        android:layout_marginEnd="12dp"
        android:backgroundTint="#B91C1C"
        android:elevation="12dp"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:text="SYNC DIAGNOSTICS"
        android:textAllCaps="false"
        android:textColor="#FFFFFF"
        android:textSize="11sp" />
</FrameLayout>
